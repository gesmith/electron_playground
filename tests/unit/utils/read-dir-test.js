import readDir from 'electron-playground/utils/read-dir';
import { module, test } from 'qunit';

module('Unit | Utility | read dir');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = readDir();
  assert.ok(result);
});
